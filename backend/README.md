# WriteWise AI Backend

FastAPI foundation for the WriteWise AI backend.

## Run locally

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

The API exposes `GET /` and `GET /health`.
